id = "REQ-013"
version = 5
feat = "FEAT-Commands"
hash = "b7c46a27"
status = "active"
statement = "The system must provide a weft init command that scaffolds docs/prds/, a design-decisions directory, and the embedded agent-tools skills into the AI-provider-specific directory for the current project."
acceptance = [
    "Given weft init is run in an empty project, then docs/prds/ and docs/decisions/ are created.",
    "Given weft init detects a Claude Code project (presence of .claude/), then it installs the embedded skills into .claude/skills/.",
    "Given weft init detects a Copilot project (presence of .github/copilot-instructions.md or .github/copilot/), then it installs the embedded agent tools into the appropriate Copilot directory.",
    "Given weft init is run a second time, then it does not overwrite existing files and exits 0.",
]
