id = "REQ-013"
version = 2
feat = "FEAT-Commands"
hash = "41174961"
status = "active"
statement = "The system must provide a weft init command that scaffolds docs/prds/, a design-decisions directory, and the weft skills into the current project."
acceptance = [
    "Given weft init is run in an empty project, then docs/prds/ and docs/decisions/ are created.",
    "Given weft init is run a second time, then it does not overwrite existing files and exits 0.",
]
