id = "REQ-026"
version = 2
feat = "FEAT-Skills"
hash = "f2ba6521"
status = "active"
statement = "Each issue generated by to-smart-issues must embed the REQ_ID, current version, and current hash for every requirement in its slice."
acceptance = [
    "Given an issue generated by to-smart-issues, then its body contains REQ-NNN vN <hash> for each requirement in the slice.",
    "Given the trace annotation format, the embedded values must be copy-paste correct for use in @implements and @verifies annotations.",
]
